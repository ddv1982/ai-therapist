/**
 * @deprecated Memory context types now live in lib/therapy prompts and API routes.
 * Keeping type export to avoid breaking older imports until cleanup completes.
 */
export type MemoryContext = never;