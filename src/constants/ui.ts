/**
 * UI Constants
 * Timing values shared across UI animations and debounced actions
 */

// Timing constants (milliseconds)
export const TIMING = {
  DEBOUNCE: {
    FAST: 300,
    DEFAULT: 500,
    SLOW: 1000,
  },
} as const;
