#!/bin/sh

# Pre-commit hook: Full QA suite (validation only, no auto-formatting)

# Find bun binary
BUN=$(command -v bun 2>/dev/null || echo "$HOME/.bun/bin/bun")

echo "🔍 Pre-commit: Running full QA suite..."

$BUN run qa:full || exit 1

echo "✅ Pre-commit checks passed!"
