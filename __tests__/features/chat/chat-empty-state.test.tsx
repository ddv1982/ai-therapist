import { ChatEmptyState } from '@/features/chat/components/dashboard/chat-empty-state';

describe('ChatEmptyState', () => {
  it('exposes a React component', () => {
    expect(typeof ChatEmptyState).toBe('function');
  });
});
